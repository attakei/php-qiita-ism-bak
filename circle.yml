machine:
  php:
    version: 5.6.14
  timezone: Asia/Tokyo
test:
  pre:
    - cp .env.example .env
    - php artisan key:generate
deployment:
  production:
    branch: master
    commands:
      - bash ./deploy.sh
